import { createTheme } from '@mui/material/styles'
const theme = createTheme({
  palette: {
    primary: {
      main: '#2da6ca',
    },
    secondary: {
      main: '#ffffff',
    },
    grey:{
      main:'#767676',
    }
  },
})

export default theme
